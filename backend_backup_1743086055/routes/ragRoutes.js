const express = require("express"); const router = express.Router(); const auth = require("../middleware/auth"); const getRagDocuments = async (req, res) => { try { res.json([]); } catch (err) { res.status(500).json({ message: "Server error" }); } }; router.get("/", auth, getRagDocuments); module.exports = router;
